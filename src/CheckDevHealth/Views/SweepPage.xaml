<Page
    x:Class="CheckDevHealth.Views.SweepPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
            <TextBlock Text="Check Dev Health" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Button x:Name="RunButton" Content="Run Sweep" Click="RunButton_Click" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Center" />
            <ProgressRing x:Name="Progress" IsActive="False" Width="24" Height="24" VerticalAlignment="Center" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Opacity="0.7" />
        </StackPanel>

        <InfoBar x:Name="SummaryBar" Grid.Row="1" Margin="0,0,0,12" IsOpen="False" IsClosable="False" />

        <ScrollViewer Grid.Row="2">
            <ItemsRepeater x:Name="ResultsRepeater">
                <ItemsRepeater.Layout>
                    <StackLayout Spacing="16" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local1:CategoryGroup" xmlns:local1="using:CheckDevHealth.Views">
                        <StackPanel Spacing="6">
                            <TextBlock Text="{x:Bind CategoryName}" FontSize="16" FontWeight="SemiBold" />
                            <ItemsControl ItemsSource="{x:Bind Items}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="local1:CheckResultViewModel">
                                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                CornerRadius="6" Padding="12" Margin="0,0,0,4">
                                            <Grid ColumnSpacing="12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <FontIcon Grid.Column="0" Glyph="{x:Bind Glyph}" Foreground="{x:Bind StatusBrush}" VerticalAlignment="Top" />
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                    <TextBlock Text="{x:Bind Value}" TextWrapping="Wrap" Opacity="0.85" />
                                                    <TextBlock Text="{x:Bind Recommendation}" TextWrapping="Wrap"
                                                               Foreground="{ThemeResource SystemFillColorCautionBrush}"
                                                               Visibility="{x:Bind HasRecommendation}" />
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
