<Page
    x:Class="CheckDevHealth.Views.AnalysisPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="AI Analysis" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
            <Button x:Name="AnalyzeButton" Content="Analyze with AI" Click="AnalyzeButton_Click" Style="{StaticResource AccentButtonStyle}" />
            <ProgressRing x:Name="Progress" IsActive="False" Width="24" Height="24" />
            <TextBlock x:Name="ModeText" VerticalAlignment="Center" Opacity="0.7" />
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <TextBlock x:Name="ResultText" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
        </ScrollViewer>
    </Grid>
</Page>
